{
  "name": "neon-hoops-expo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web",
    "sync:game": "node scripts/sync-local-game.mjs",
    "eas:build:ios:sim": "npx eas build -p ios --profile development",
    "eas:build:ios:preview": "npx eas build -p ios --profile preview",
    "eas:build:ios": "npx eas build -p ios --profile production",
    "eas:build:android:apk": "npx eas build -p android --profile preview",
    "eas:build:android": "npx eas build -p android --profile production",
    "eas:submit:ios": "npx eas submit -p ios --latest"
  },
  "dependencies": {
    "expo": "~54.0.33",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "react-native-webview": "^13.15.0"
  },
  "private": true
}
